<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Elite Math & Physics Tutoring</title>
    <style>
        :root {
            --primary-dark: #0f172a;
            --primary-light: #1e293b;
            --accent: #f59e0b;
            --text-main: #f8fafc;
            --text-muted: #94a3b8;
        }
        
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', Roboto, Helvetica, Arial, sans-serif;
        }

        body {
            background-color: var(--primary-dark);
            color: var(--text-main);
            line-height: 1.6;
        }

        /* Navigation */
        header {
            background-color: var(--primary-light);
            padding: 1.5rem 5%;
            display: flex;
            justify-content: space-between;
            align-items: center;
            border-bottom: 1px solid #334155;
        }

        .logo {
            font-size: 1.5rem;
            font-weight: bold;
            color: var(--text-main);
        }

        .logo span {
            color: var(--accent);
        }

        nav a {
            color: var(--text-main);
            text-decoration: none;
            margin-left: 2rem;
            font-weight: 500;
            transition: color 0.3s ease;
        }

        nav a:hover {
            color: var(--accent);
        }

        /* Hero Section */
        .hero {
            text-align: center;
            padding: 8rem 5%;
            background: linear-gradient(135deg, var(--primary-dark) 0%, var(--primary-light) 100%);
        }

        .hero h1 {
            font-size: 3.5rem;
            margin-bottom: 1.5rem;
            letter-spacing: -0.05em;
        }

        .hero p {
            font-size: 1.25rem;
            color: var(--text-muted);
            max-width: 600px;
            margin: 0 auto 2.5rem;
        }

        .btn {
            display: inline-block;
            background-color: var(--accent);
            color: var(--primary-dark);
            padding: 1rem 2.5rem;
            border-radius: 5px;
            text-decoration: none;
            font-weight: bold;
            font-size: 1.1rem;
            transition: transform 0.2s ease, box-shadow 0.2s ease;
        }

        .btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 12px rgba(245, 158, 11, 0.3);
        }

        /* Services Grid */
        .services {
            padding: 5rem 5%;
            text-align: center;
        }

        .services h2 {
            font-size: 2.5rem;
            margin-bottom: 3rem;
        }

        .grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 2rem;
            max-width: 1200px;
            margin: 0 auto;
        }

        .card {
            background-color: var(--primary-light);
            padding: 2.5rem;
            border-radius: 8px;
            border: 1px solid #334155;
            transition: transform 0.3s ease;
        }

        .card:hover {
            transform: translateY(-5px);
        }

        .card h3 {
            color: var(--accent);
            font-size: 1.5rem;
            margin-bottom: 1rem;
        }

        .card p {
            color: var(--text-muted);
        }

        /* Footer */
        footer {
            text-align: center;
            padding: 2rem 5%;
            background-color: var(--primary-light);
            color: var(--text-muted);
            border-top: 1px solid #334155;
        }
    </style>
</head>
<body>

    <header>
        <div class="logo">Axiom<span>Tutoring</span></div>
        <nav>
            <a href="#about">About</a>
            <a href="#services">Services</a>
            <a href="#contact">Contact</a>
        </nav>
    </header>

    <section class="hero">
        <h1>Master the Mechanics of the Universe.</h1>
        <p>Premium, personalized tutoring in advanced Mathematics and Physics. We turn complex concepts into clear, intuitive understanding.</p>
        <a href="#contact" class="btn">Book a Consultation</a>
    </section>

    <section id="services" class="services">
        <h2>Areas of Expertise</h2>
        <div class="grid">
            <div class="card">
                <h3>Advanced Mathematics</h3>
                <p>Comprehensive support for Calculus (AB/BC, Multivariable), Linear Algebra, Differential Equations, and advanced Statistics.</p>
            </div>
            <div class="card">
                <h3>Theoretical & Applied Physics</h3>
                <p>Mastery in Classical Mechanics, Electromagnetism, Thermodynamics, and AP/College-level General Physics.</p>
            </div>
            <div class="card">
                <h3>Exam Preparation</h3>
                <p>Targeted strategies for AP Physics, AP Calculus, SAT Math, and university-level final examinations.</p>
            </div>
        </div>
    </section>

    <footer>
        <p>&copy; 2026 Axiom Tutoring. Elevating education.</p>
    </footer>

</body>
</html>